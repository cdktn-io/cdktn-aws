# Copyright (c) cdktn-io
# SPDX-License-Identifier: MPL-2.0
#
# The pull-request gate. Every check here ran by hand through M1-M3 stage 1; this file is the
# stage-2 job of making it run on every change.
#
# THIS WORKFLOW HAS NEVER EXECUTED. It was authored in M3 stage 2 and the repository has no pushed
# remote branches, so no run has ever been scheduled: every command below is one that was run by
# hand locally, transcribed. Treat the first real run as the workflow's own smoke test — expect
# setup-level breakage (action versions, cache keys, runner tool versions) that local runs cannot
# have surfaced.
#
# What is deliberately NOT here, and why:
#
#   * The full 258-group `jsii-pacmak --targets go` pack plus the whole-fleet `zip.CheckDir` size
#     gate. Packing the fleet is ~880 CPU-seconds and produces ~540 MiB; even on the eight-core
#     Depot runners the pack shards use, that is minutes added to every PR to re-prove a bound whose
#     worst module sits at 11.6 % of the cap with 8.6x of headroom. A PR packs and measures two of
#     the eight shards, and `fleet-full.yml` does all eight nightly, on the `full-fleet` label, and
#     on demand.
#   * Anything that pushes, tags or publishes. `scripts/release.mjs` is dry-run-only by
#     construction and no workflow runs it.
#
# The schema-fed gates are one job on purpose: the ~34 MB `terraform providers schema -json` dump
# is what `check:groups`, `pnpm mine`, `pnpm generate` and the strict contract diff all need, and
# splitting them would pay for it four times.
name: ci

on:
  pull_request:
  push:
    branches: [main]

# One in-flight run per ref: these jobs are long and a superseded PR revision has nothing to say.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Least privilege by default; every job below declares exactly the permissions it needs. Nothing in
# either workflow writes to the repository, so every job is `contents: read` — but declared per job,
# so adding one that does need more is a visible edit rather than an inherited grant.
permissions: {}

# The check contexts branch protection can require are the `name:` values below, not the job keys,
# and the matrix jobs expand one context per shard. In full, on a pull request:
#
#   typecheck, tests and invariants
#   groups, generate determinism and runtime contract
#   synth smoke (validation on)
#   jsii shard 1/8 … jsii shard 8/8
#   every group compiled exactly once, JSII3/JSII6 zero
#   pacmak + size shard 1/8, pacmak + size shard 5/8
#   PR size coverage (2 of 8 shards)
#
# cdktn-repository-manager's `CustomConstructsStack` entry for this repo sets `protectMainChecks`,
# and with `enforce_admins: true` a required context that no job produces makes every PR — admin
# merges included — permanently unmergeable. That is exactly what round 2 of that repo's PR #83
# caught for cdktn-awscc. **Any rename below is a change to the manager too.** The two must be
# reconciled before the manager's adoption PR is applied, not after.
#
# The manager requires the five non-matrix names only, because the shard names are matrix-expanded
# contexts that go stale the moment the shard count changes. That is safe *only* because the two
# reconcile jobs below carry `if: ${{ !cancelled() }}` plus an explicit `needs.*.result` gate: a
# job skipped by an unmet `needs:` is reported to branch protection as passing, so without that
# pair a red shard would leave every required context green.
jobs:
  # ---------------------------------------------------------------------------------------------
  # The gates that need nothing but the checkout: no provider schema, no Go toolchain, no jsii.
  checks:
    name: typecheck, tests and invariants
    # Fleet convention: the heavy jobs run on Depot's 8-core/32 GB runners, the light ones stay on
    # GitHub's standard two-core/16 GB image. Heavy here means the fleet-wide compiles — this job
    # typechecks all 258 generated packages, which is cores and resident memory, not I/O.
    runs-on: depot-ubuntu-24.04-8
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - name: typecheck (tools, and all 258 generated packages separately)
        run: pnpm typecheck

      - name: contract, scale, manifest and inventory tests
        # `manifests.test.ts` covers VERDICT risks #4 and #5 — the 258 machine-generated manifests
        # whose three permanent strings are unrepairable after the first Go tag, and the four
        # inventories that have to describe one group set.
        #
        # CDKTN_AWS_GO_ROOT=none is the test's explicit "this runner has no Go checkout"
        # declaration: cdktn-aws-go has no remote a runner can clone yet, so its 259 assertions
        # skip here. Without the declaration the test *fails* on a CI runner rather than skipping,
        # so an unset or wrong CDKTN_AWS_GO_ROOT can never pass as green. DELETE THIS LINE and
        # check the repo out here the moment it has a reachable remote.
        env:
          CDKTN_AWS_GO_ROOT: none
        run: pnpm test

      - name: no cross-group imports (TypeScript)
        run: pnpm check:imports

  # ---------------------------------------------------------------------------------------------
  # Everything that reads the pinned provider schema.
  schema-gates:
    name: groups, generate determinism and runtime contract
    # Depot: the mine/check gates are light, but this job generates the whole 258-group tree twice
    # and builds the contract baseline, which is the CPU-bound half.
    runs-on: depot-ubuntu-24.04-8
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - uses: $/.github/actions/provider-schema

      - name: check:groups (gate A coverage, gate B moves, canonical form)
        run: pnpm check:groups

      - name: cache the provider website checkout the miner clones
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: tmp/terraform-provider-aws
          key: provider-website-${{ hashFiles('schemas/PROVIDER_VERSION') }}

      - name: mine twice — byte-identical, and equal to the committed groups.json
        # groups.json is the source of truth and the miner only ever proposes a diff against it
        # (docs/provider-bump-runbook.md). On a PR that is not a bump the proposal must be empty,
        # and it must be empty the same way twice: a miner that is not deterministic would make
        # every future bump diff unreadable.
        run: |
          cp groups.json "${RUNNER_TEMP}/groups.committed.json"
          pnpm mine
          cmp "${RUNNER_TEMP}/groups.committed.json" groups.json
          pnpm mine
          cmp "${RUNNER_TEMP}/groups.committed.json" groups.json
          git diff --exit-code -- groups.json

      - name: generate twice — byte-identical, and equal to the committed tree
        # `hashes.json` is what the Go release tags off, so a generator that is not a pure function
        # of (groups.json, schema, generator) would re-tag 258 modules for nothing.
        run: |
          pnpm generate
          git diff --exit-code -- generated/
          pnpm generate
          git diff --exit-code -- generated/

      - name: runtime contract diff (strict)
        # Without --strict a missing reference build is a skip with exit 0, which is right for a
        # fresh clone and wrong here: this job has the schema dump, so it can always build the
        # baseline reference, and a skip would only teach us to ignore the check.
        run: node scripts/runtime-contract-diff.mjs --strict

  # ---------------------------------------------------------------------------------------------
  # The one end-to-end check: two independently compiled group assemblies plus the provider
  # assembly, loaded into one program, synthesised with cdktn's validations ON.
  synth-smoke:
    name: synth smoke (validation on)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - name: compile the three groups the smoke test loads
        # synth:smoke needs their lib/ and names this command when it is absent — running it here
        # keeps a failure about the test rather than about the setup.
        run: node scripts/build-generated.mjs provider elb lambda

      - name: synth:smoke
        run: pnpm synth:smoke

  # ---------------------------------------------------------------------------------------------
  # Every group compiled by the one pinned jsii. Sharded because the groups are independent by
  # construction — zero cross-group imports, one assembly each — so this is embarrassingly parallel
  # and the only limit is how many runners we ask for.
  fleet-jsii:
    name: jsii shard ${{ matrix.shard }}/8
    # Depot: jsii holds a whole group assembly in memory per compile, eight legs of them.
    runs-on: depot-ubuntu-24.04-8
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6, 7, 8]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - name: jsii compile, shard ${{ matrix.shard }} of 8
        # Shards are dealt largest-first by committed source bytes, so every leg carries the same
        # ~10.6 MiB and the matrix costs its average rather than its worst group.
        run: node scripts/build-fleet.mjs --jsii --shard ${{ matrix.shard }}/8

      - name: JSII3 and JSII6 must be zero
        # build-fleet already fails any group that emits either, but the M3 bar is stated as fleet
        # totals — assert them from the run record so the numbers are in the log, not inferred.
        run: |
          node -e '
            const r = require("./tmp/m3/fleet-jsii.json");
            const j3 = r.codeTotals.JSII3 ?? 0;
            const j6 = r.codeTotals.JSII6 ?? 0;
            console.log(`${r.okCount}/${r.groups} OK, JSII3 ${j3}, JSII6 ${j6}`);
            if (r.okCount !== r.groups || j3 !== 0 || j6 !== 0) process.exit(1);
          '

      - name: upload the shard's compile record
        # The reconciliation job below needs the per-group keys to prove the eight shards partition
        # the fleet. Without this every shard is green on its own and nobody adds them up.
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: jsii-shard-${{ matrix.shard }}
          path: tmp/m3/fleet-jsii.json
          if-no-files-found: error

  # ---------------------------------------------------------------------------------------------
  # Eight green shards are not the same claim as "the fleet compiles". A shard that silently
  # compiled nothing reports 0/0 OK and exits 0; a shard-dealing bug that dropped a group from every
  # leg would too. This job is the only place the fleet-level claim is actually made.
  fleet-jsii-reconcile:
    name: every group compiled exactly once, JSII3/JSII6 zero
    needs: fleet-jsii
    # GitHub counts a *skipped* required check as passing, and a `needs:` job whose dependency
    # failed is published as skipped — so the plain default would make this context green whenever
    # a shard went red, i.e. advisory in exactly the case it exists to catch. The manager requires
    # this name (and not the eight shard names), so it has to stand in for them: `!cancelled()`
    # runs it whatever the shards did, and the first step below turns a red shard into a red
    # required context. A cancelled run is still skipped on purpose — nothing was proven or
    # disproven there, and cancellation is not a result to gate on.
    if: ${{ !cancelled() }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: the eight shards must have passed
        # Not redundant with the partition check below: a shard uploads its record `if: always()`,
        # so it can leave a complete, self-consistent artifact and still have failed — the JSII3/
        # JSII6 assertion runs after the compile, and any earlier step can fail before either.
        env:
          SHARDS_RESULT: ${{ needs.fleet-jsii.result }}
        run: |
          echo "fleet-jsii matrix result: $SHARDS_RESULT"
          [ "$SHARDS_RESULT" = "success" ] || exit 1
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      # No merge-multiple: every shard's file is named fleet-jsii.json, so merging would leave one
      # of the eight and the count would look right for the wrong reason.
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: jsii-shard-*
          path: shards
      - name: the eight shards partition generated/, and add up to zero JSII3/JSII6
        run: |
          node -e '
            const fs = require("node:fs");
            const dirs = fs.readdirSync("shards").sort();
            if (dirs.length !== 8) {
              throw new Error(`expected 8 shard records, got ${dirs.length}: ${dirs.join(", ")}`);
            }
            // hashes.json is the inventory the release step tags off, so it is the right
            // definition of "the fleet" to reconcile against — not a hardcoded 258.
            const expected = new Set(
              Object.keys(JSON.parse(fs.readFileSync("generated/hashes.json", "utf-8")).groups),
            );
            const seen = new Map();
            let j3 = 0, j6 = 0, ok = 0;
            for (const d of dirs) {
              const r = JSON.parse(fs.readFileSync(`shards/${d}/fleet-jsii.json`, "utf-8"));
              j3 += r.codeTotals.JSII3 ?? 0;
              j6 += r.codeTotals.JSII6 ?? 0;
              ok += r.okCount;
              for (const g of Object.keys(r.perGroupMs)) {
                if (seen.has(g)) throw new Error(`${g} compiled by both ${seen.get(g)} and ${d}`);
                seen.set(g, d);
              }
            }
            const missing = [...expected].filter((g) => !seen.has(g)).sort();
            const extra = [...seen.keys()].filter((g) => !expected.has(g)).sort();
            console.log(`${seen.size}/${expected.size} groups compiled, ${ok} OK, JSII3 ${j3}, JSII6 ${j6}`);
            if (missing.length) console.error(`never compiled by any shard: ${missing.join(", ")}`);
            if (extra.length) console.error(`compiled but not in hashes.json: ${extra.join(", ")}`);
            if (missing.length || extra.length || ok !== expected.size || j3 !== 0 || j6 !== 0) {
              process.exit(1);
            }
          '

  # ---------------------------------------------------------------------------------------------
  # Pack to Go and measure, on two of the eight shards. `check-go-size` calls x/mod/zip.CheckDir —
  # the same function `go mod download` runs — so this is the proxy's decision, not an estimate.
  fleet-pack-size:
    name: pacmak + size shard ${{ matrix.shard }}/8
    # Depot: jsii + pacmak --targets go over a shard is the most expensive thing a PR runs.
    runs-on: depot-ubuntu-24.04-8
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        # Two representative eighths per PR. The deal is size-balanced and deterministic, so each
        # shard carries a mix of giants and tiny groups and the same shard means the same groups on
        # every run; shard 1 is where `lex_v2_models` — the 60 MB worst case, 11.6 % of the cap —
        # lands. The other six are covered by fleet-full.yml.
        shard: [1, 5]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 22
          cache: pnpm
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5.6.0
        with:
          go-version: "1.25"
          cache-dependency-path: tools/gosize/go.sum
      - run: pnpm install --frozen-lockfile

      - name: jsii + jsii-pacmak --targets go, shard ${{ matrix.shard }} of 8
        run: node scripts/build-fleet.mjs --shard ${{ matrix.shard }}/8

      - name: no cross-group imports in the packed Go
        # The TypeScript check proves the invariant for source we write; this proves it for code
        # jsii-pacmak writes, which is where Option B's shape (one shared `core` every group
        # imports) would appear if it were going to appear anywhere.
        run: node scripts/check-go-module-isolation.mjs

      - name: zip.CheckDir size gate
        run: node scripts/check-go-size.mjs --json tmp/m3/sizes-shard-${{ matrix.shard }}.json

      - name: upload the size records
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: go-size-shard-${{ matrix.shard }}
          path: tmp/m3/
          if-no-files-found: ignore

  # ---------------------------------------------------------------------------------------------
  # The two size shards, added up in one place — so a green PR states what it actually proved. This
  # is a partial gate by design (six eighths are fleet-full.yml's job) and the summary says so out
  # loud, because "PR green" must never be read as "258 modules under the cap".
  go-size-reconcile:
    name: PR size coverage (2 of 8 shards)
    needs: fleet-pack-size
    # Same reason as fleet-jsii-reconcile above: a `needs:`-skipped job reports as passing to
    # branch protection, and this is the only required context standing for the two size shards.
    if: ${{ !cancelled() }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: both size shards must have passed
        env:
          SHARDS_RESULT: ${{ needs.fleet-pack-size.result }}
        run: |
          echo "fleet-pack-size matrix result: $SHARDS_RESULT"
          [ "$SHARDS_RESULT" = "success" ] || exit 1
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: go-size-shard-*
          path: shards
          merge-multiple: true
      - name: both shards measured something, nothing is over the cap, and the gap is named
        run: |
          node -e '
            const fs = require("node:fs");
            const files = fs.readdirSync("shards").filter((f) => /^sizes-shard-\d+\.json$/.test(f)).sort();
            if (files.length !== 2) {
              throw new Error(`expected the 2 PR shard records, got ${files.length}: ${files.join(", ")}`);
            }
            const shards = files.map((f) => JSON.parse(fs.readFileSync(`shards/${f}`, "utf-8")));
            const cap = shards[0].cap;
            const records = shards.flatMap((s) => s.records);
            const seen = new Set();
            for (const r of records) {
              if (seen.has(r.module)) throw new Error(`${r.module} measured by both shards`);
              seen.add(r.module);
            }
            // A shard that packed nothing exits 0 with an empty record set, which would otherwise
            // read as "0 over the cap".
            if (records.length === 0) throw new Error("the shards measured no modules at all");
            const total = Object.keys(
              JSON.parse(fs.readFileSync("generated/hashes.json", "utf-8")).groups,
            ).length;
            records.sort((a, b) => b.totalValidBytes - a.totalValidBytes);
            const worst = records[0];
            const pct = ((100 * worst.totalValidBytes) / cap).toFixed(2);
            console.log(`shards 1 and 5: ${records.length} of ${total} modules measured`);
            console.log(`worst here: ${worst.module} ${worst.totalValidBytes} B = ${pct} % of the ${cap} B cap`);
            console.log(
              `NOT PROVEN BY THIS RUN: the other ${total - records.length} modules. ` +
                "fleet-full.yml measures all 258 nightly, on the `full-fleet` label, and on demand.",
            );
            const over = records.filter((r) => !r.pass);
            for (const r of over) console.error(`OVER CAP ${r.module} ${r.totalValidBytes} B`);
            if (over.length > 0) process.exit(1);
          '
